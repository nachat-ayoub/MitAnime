import React from "react";

const PageNotFound = () => {
  return (
    <div
      style={{
        minHeight: "calc(100vh - 100px)",
      }}
      className="page-not-found d-flex justify-content-center align-items-center felx-column h-100 w-100"
    >
      <h2 className="text-white">Page Not Found {" ):"}</h2>
    </div>
  );
};

export default PageNotFound;
